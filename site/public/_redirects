# Cloudflare Pages 重写规则（Next.js 静态导出的 RSC 预取载荷）
#
# ⚠️ **这两条规则目前是空转的，别以为它们在修什么**（2026-09-13 实测，新构建上线后）。
#
# 背景：Next 的静态导出把段级预取载荷落盘在带**字面 `$`** 的目录里：
#     site/out/docs/guide/quick-start/__next.docs/$oc$slug/__PAGE__.txt   ← 文件确实在产物里
# 而客户端预取请求用的是点号压平形式：
#     /docs/guide/quick-start/__next.docs.$oc$slug.__PAGE__.txt
# 于是有人加了下面两条规则，把点号形式 200 重写到嵌套形式。
#
# 2026-09-13（Cloudflare 新构建上线后）逐条 curl 复测，结论与注释原先的假设相反：
#
#   /docs/guide/quick-start/__next.docs.txt                          → 200  ✅
#   /docs/guide/quick-start/index.txt                                → 200  ✅
#   /docs/guide/quick-start/__next.docs/$oc$slug/__PAGE__.txt        → 404  ❌（字面 $）
#   /docs/guide/quick-start/__next.docs/%24oc%24slug/__PAGE__.txt    → 404  ❌（百分号编码）
#   /docs/guide/quick-start/__next.docs.$oc$slug.__PAGE__.txt        → 404  ❌（点号压平）
#   /__next.!KGhvbWUp/__PAGE__.txt                                   → 404  ❌
#
# 即：**文件在构建产物里存在，但 Cloudflare 不服务 `$oc$slug/` 这条路径**。
# 所以本文件的两条规则把「一个 404」重写成「另一个 404」，不可能修好预取；
# 原始的「嵌套形式 200」很可能是本地 `python3 -m http.server --directory site/out`
# 测出来的（本地文件服务器不介意 `$`，Cloudflare 介意）。
#
# 影响面很小：软导航本身正常（走 `index.txt` / `__next.*.txt`，都是 200），
# 只是控制台会多一条预取 404，预取退化为按需拉取。
#
# 要真正修掉，只有两条路（都还没做，先记录）：
#   a) 写一个 Pages Function：把点号形式的请求用 ASSETS 绑定映射到那条 `$` 路径
#      （即由我们自己读产物文件，而不是指望静态服务器命中它）；
#   b) 向 Next / Cloudflare 反馈「`$` 目录名在 Pages 静态资源上 404」，
#      等上游改掉命名或支持该路径。
#
# 在下决心之前保留规则原文，但请知道：它们既是无效的，也**不要**再往这条
# 思路上加规则——重写目标本身不可达。
# 规则只匹配 __next. 前缀，不会影响任何正常路由。
/docs/*/__next.docs.$oc$slug.__PAGE__.txt  /docs/:splat/__next.docs/$oc$slug/__PAGE__.txt  200
/__next.!KGhvbWUp.__PAGE__.txt  /__next.!KGhvbWUp/__PAGE__.txt  200
