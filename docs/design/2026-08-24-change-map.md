# 轻屿课表 · 本次 UI 改造「原来 → 现在」全量对照表

> 基线 commit e697c7b（改造前）→ 当前 HEAD。共 49 文件、+2132/-1982 行。
> 结论先行：**没有功能被无声丢弃**——每一处变化要么是迁移（给了新位置）、要么是有意移除（下表注明恢复方式）、要么是修复过程中的真失误（已标注 ✅ 已修正）。

## 一、入口与导航

| 功能 | 原来在哪 | 现在在哪 | 说明 |
|---|---|---|---|
| 底栏「设置」Tab | 玻璃坞第 3 个 Tab | 底栏编辑器里可作为按钮添加（内嵌页，坞常驻）；⋮ 菜单也有 | 先移除后按用户需求以「5 槽自由编排」形式回归 |
| 底栏 日/周 Tab 固定 | 布尔开关控制显隐 | 底栏编辑器自由编排（最多 5 槽，含全部目录条目） | 旧配置自动迁移为初始排列 |
| 圆钮自定义功能（回本周等） | 可从目录任选 | 固定为加课程 | 需要恢复可编排就说一声 |
| ⋮ 菜单列表形态 | 列表 / 八宫格双形态 | 八宫格唯一形态 | 双形态维护成本高 |
| 八宫格编辑器入口 | 设置→外观→自定义八宫格 | 设置→首页与导航→右上角菜单 | 曾漏迁，d674208 已修 |
| 回本周小字（星期栏内嵌） | 时间栏下方常驻小字 | 移除；右下浮钮为唯一入口 | 三重入口收敛为一 |
| 启动自动弹更新窗 | 有新版即弹 | 移除；保留顶栏红点＋菜单项 | 打扰治理 |

## 二、设置信息架构

| 条目 | 原来 | 现在 |
|---|---|---|
| 时间模板 | hub＋课表页面双入口 | 两处都在（hub 一级位按用户反馈保留；同路由） |
| 超级岛自检 | 超级岛页＋诊断页双入口 | 仅诊断页（排障心智归诊断） |
| 内存状态 | 开发者组＋诊断页 | 诊断页（仅 debug/profile 包显示）；八宫格候选同门控，正式版不可见 |
| 外观页内容 | 主题/字体/导航形态/玻璃坞开关混装 | 瘦身为：预览、主题模式、字体、主题色、磨砂质感、恢复默认 |
| 新增页 | — | 首页与导航：导航形态·底栏编排·右上角菜单·首页标题·恢复默认 |
| 节假日高级页标题 | 误用云同步键「高级设置」 | 正名「节假日数据更新」 |
| 各页副题 | 8 处冗长/路线图泄漏文案 | 全部瘦身（六语言同步） |

## 三、视觉系统

| 项 | 原来 | 现在 |
|---|---|---|
| 课程橙对文字 | #E65100 对比度 2.56 不达标 | #B34700，实测 3.72 达标 |
| 危险红 | #E05D44 字面量散落 4 处 | 收编 HyperosColors.destructive 全库统一 |
| 标题字重 | 全站纯 w400 | 标题/组标签 w500 形成层级，正文不变 |
| 玻璃折射率默认 | 1.59 超自家上限 1.5（存档漂移） | 对齐 1.5 |
| 更新红点色 | Colors.redAccent | destructive 统一 |

## 四、底栏交互回归修复（本轮）

| 问题 | 根因 | 修复 |
|---|---|---|
| 白页面底栏不反色 | 内嵌态极性分支被误删 | 内嵌态只看主题明暗（c002154①） |
| 页面切换有滑入动画 | 我加的转场多余 | 改回闪现直切（c002154②） |
| 内嵌页出现无意义返回键 | 子页默认渲染 onBack | HyperosSubpageNoBack 标记隐藏（c002154③），普通推入路由不受影响 |
| 八宫格只剩课表设置 | 空排列被钉住固化＋编辑器入口缺失 | 解析器自愈回退默认八项＋入口找回（d674208） |

## 五、提交清单（按主题归组）

- 文档机器人 ×7：bece650 / cb35025 / b5f1b83 / f90cb95 / 6419148 / ff2342c 及相关 merge —— 官网 releases/schools 自动更新
- 文案 i18n ×3：1550878、9d058e2、6044f76
- 视觉令牌 ×2：b5c80d1、0168968
- 首页/底栏 ×5：8833fcd、599adf8、986e4f4、c002154、c8edddf(外部代理)
- 设置 IA ×3：664d626、d674208、b45dcac
- 测试对齐 ×1：35ec111　设计文档 ×1：f897456　超级岛字重回修 ×1：c10aeaf(外部)
- 合并提交 ×3

> 历史未做 squash/重写：主分支共享，重写需强制推送。若确要整合为少量大提交，开新分支操作并另行确认；当前代码状态以本表为准。

## 六、遗留待办（非丢失，是排期）

1. ⋮ 八宫格下的「全部功能」二级面板（三分组漏斗）
2. 背景 scope 四开关 → 所见即所得示意图
3. 玻璃模式命名预设化 ＋ 同屏玻璃面 ≤2 预算制
4. 课程随机色板与粉彩对单轨化
5. 圆角五档收敛 ＋ CI lint 门禁
6. 三个超载设置页折叠至 ≤9 条目
7. timetable_screen.dart 拆分四模块
8. 小清理：_kStockDockGlass 开关、三份重复玻璃常量、4 个失效持久字段退役
9. timetable_day_view_test 挂起排查（基线即复现）
