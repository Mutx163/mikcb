# v2.0.6

### 优化

- HyperOS PR1 边界解耦：去除 lib/ui/hyperos 对 models/providers/services/l10n 的业务 import
- 迁出课表专用 UI：`frosted_sheet_settings_preview` 和 `home_page_region_blur` 移至 `lib/widgets/`
- 新增 Motion 注入机制：`hyperos_motion.dart` + `hyperos_motion_bridge.dart` 实现平台级动画抽象
- 新增 App Bridge 解耦：`hyperos_app_bridge.dart` 隔离 app 层依赖
- FrostedAppearance 改为库内常量 + app bridge 扩展模式
- select/controls 文案参数化：实现 l10n 解耦，支持多语言动态切换
- 迁出 debug 注册：`hyperos_layout_debug_tuning.dart` 独立管理布局调试
- HyperOS PR2 拆分上帝文件：`hyperos_widgets.dart` 拆分为 `widgets/indicators.dart`、`layout.dart`、`tiles.dart`、`cards.dart` 四个子模块
- HyperOS PR3 颜色统一：扩展 `HyperosColors` 语义颜色（primary、surfaceContainer、error、outline 等）

### 调整

- 清理废弃的调试图片文件：`_debug_settings.png`、`_debug_shot.png`
- 更新 CONTRIBUTING.md 贡献指南
